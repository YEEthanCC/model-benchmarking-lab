AI Agent Benchmark

Lightweight CLI-first framework to run reproducible experiments comparing AI agents (models, tools, datasets, evaluation).

See `requirements.txt` and `cli/run_experiment.py` for usage examples.
